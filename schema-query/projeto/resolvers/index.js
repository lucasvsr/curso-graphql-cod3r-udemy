const Query = require('./Query') //Consultas
const Produto = require('./Produto') //Resolvers do type Produto
const Usuario = require('./Usuario') //Resolvers do type Usuario

module.exports = {

    Query,
    Produto,
    Usuario

}